/**
 * Compatibility export for older implant-local imports.
 *
 * The wire codec is owned by @octoc2/shared so the implant and server cannot
 * silently diverge.
 */
export { StegoCodec } from "@octoc2/shared/stego";
